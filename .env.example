# Optional: set these when you are ready to use live integrations.
GEMINI_API_KEY=
NCBI_EMAIL=
NCBI_API_KEY=

# OpenTrial defaults to deterministic demo data when live integrations are off.
OPENTRIAL_USE_LIVE_APIS=false

# Optional diagnostics and HTTP resilience.
OPENTRIAL_DEBUG=false
OPENTRIAL_HTTP_RETRIES=2
